---
name: Bug report
about: Something editsy did wrong, or didn't do
labels: bug
---

**What happened?**

**What did you expect?**

**Setup**

- editsy version (`npm ls editsy @editsy/cli @editsy/next`):
- Mode: local (`editsy edit`) / remote (`/editsy` on the deployed site)
- Host (if remote): Vercel / Netlify / self-hosted / other
- Node version, OS:

**The content file involved (or a trimmed version that reproduces it)**

```ts
```

If setup is the problem, paste `npx editsy doctor`'s output (it never
prints secret values).
