{
  "name": "editsy-monorepo",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types"
  }
}
